# Environment variables required to run the conversion service
# Replace the placeholder values with the credentials for your PostgreSQL instance.
POSTGRES_SERVER=postgres-hostname
POSTGRES_USER=postgres-username
POSTGRES_PASSWORD=postgres-password
POSTGRES_DATABASE=postgres-database
