# Configuración de la base de datos PostgreSQL utilizada por la API de conversión.
# Dirección (host o host:puerto) del servidor PostgreSQL accesible desde la aplicación FastAPI.
POSTGRES_SERVER=postgres
# Usuario con permisos de lectura y escritura sobre los esquemas tenant que exponen las tablas utilizadas.
POSTGRES_USER=msw_user
# Contraseña del usuario anterior.
POSTGRES_PASSWORD=msw_password
# Nombre de la base de datos donde residen los esquemas de cada tenant.
POSTGRES_DATABASE=msw_conversion
